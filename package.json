{
  "name": "miayaar-platform",
  "version": "0.1.0",
  "private": true,
  "description": "MIAYAAR Decision Intelligence Platform",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && cd dist && node --test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}